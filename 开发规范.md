# CodeAtlas（暂定名）—— AI代码地图

> 一个帮助非程序员理解代码项目结构的桌面软件。

版本：v0.1 MVP

---

# 一、项目目标

## 核心使命

让没有计算机背景的用户，在使用 Cursor、Claude Code、Codex、ChatGPT 等 AI 辅助编程时，可以快速知道：

- 当前文件是干什么的
- 当前文件夹负责什么功能
- AI 修改了哪些文件
- 每个修改会影响哪些地方
- 整个项目如何组织

目标不是阅读代码，而是阅读项目。

最终效果类似 Google Maps，只不过地图对象变成代码项目。

---

# 二、用户场景

典型用户：

- AI辅助编程小白
- 独立开发者
- 产品经理
- 设计师
- 第一次接触开源项目的人

典型流程：

用户打开项目目录。

例如：

```
MusicTimer/
├── app/
├── components/
├── hooks/
├── utils/
├── package.json
└── README.md
```

软件自动生成：

> app：整个应用入口

> components：界面组件

> hooks：共享逻辑

> utils：工具函数

点击任何文件，都能看到人话解释。

---

# 三、核心功能

## MVP必须完成

### 1. 项目扫描

输入：

一个文件夹。

输出：

- 完整目录树
- 文件数量
- 文件类型
- 项目类型

例如：

```
React Native Expo项目
共124个文件
TypeScript占83%
```

---

### 2. 文件语言识别

自动识别：

| 后缀 | 语言 |
|------|------|
| ts | TypeScript |
| tsx | React |
| js | JavaScript |
| py | Python |
| cpp | C++ |
| java | Java |
| json | 配置 |
| yaml | 配置 |

要求：

不仅识别后缀，还要识别文件内容。

---

### 3. 文件作用解释

输出格式统一：

```
文件：
Button.tsx

语言：
TypeScript React

作用：
负责显示按钮。

提供功能：
- 点击事件
- 样式控制

影响范围：
首页
训练页面
```

要求：

不直接总结全文。

先分析结构，再生成解释。

---

### 4. 文件夹作用解释

例如：

```
components/
```

解释：

> 存放所有可重复使用的界面组件。

再例如：

```
hooks/
```

解释：

> 存放多个页面共享的逻辑。

要求：

结合目录内文件推断。

---

### 5. AI修改翻译

读取Git Diff。

例如：

```
Button.tsx
```

输出：

> AI修改了按钮颜色。

例如：

```
timer.ts
```

输出：

> 修复暂停逻辑。

最终生成：

| 文件 | 人话 |
|------|------|
| Button | 修改样式 |
| Timer | 修复暂停 |
| App | 调整导航 |

这是MVP重点。

---

# 四、后续功能（V2）

- 项目关系图
- 点击节点跳转
- 搜索“按钮在哪里”
- 搜索“登录逻辑在哪”
- 本地缓存解释
- 多模型支持
- VSCode插件
- Cursor插件

---

# 五、技术架构

## 为什么选择Electron

原因：

- Windows桌面支持成熟
- Node可以直接扫描文件
- React方便构建UI
- TypeScript生态完善
- 后续可迁移VSCode插件

最终架构：

```
Electron
├── Main Process
├── Renderer
└── Node Services
```

---

# 六、整体架构图

```
用户
 │
 ▼
Electron界面
 │
 ▼
目录扫描器
 │
 ▼
静态分析器
 │
 ▼
项目索引
 │
 ▼
AI解释器
 │
 ▼
可视化展示
```

数据流：

```
文件
 ↓
扫描
 ↓
AST分析
 ↓
结构信息
 ↓
LLM解释
 ↓
缓存
 ↓
UI展示
```

---

# 七、目录结构

建议：

```
CodeAtlas/
├── src/
│   ├── main/
│   ├── renderer/
│   ├── scanner/
│   ├── parser/
│   ├── analyzer/
│   ├── ai/
│   ├── git/
│   ├── cache/
│   └── shared/
├── assets/
├── docs/
└── package.json
```

---

# 八、模块拆解

---

## 模块一：目录扫描器

目录：

```
src/scanner
```

职责：

递归扫描。

输入：

```
D:\Project
```

输出：

```json
{
 "name":"src",
 "children":[]
}
```

任务：

- [ ] 递归扫描
- [ ] 忽略node_modules
- [ ] 忽略.git
- [ ] 统计文件数量
- [ ] 分类文件类型

完成标准：

能够生成完整树。

---

## 模块二：语言识别器

目录：

```
src/parser
```

职责：

识别语言。

工具：

Tree-sitter。

需要识别：

- TypeScript
- JavaScript
- Python
- Java
- C++
- C#
- Go
- Rust

输出：

```json
{
 "language":"TypeScript"
}
```

---

## 模块三：AST分析器

目录：

```
src/analyzer
```

职责：

提取结构。

需要提取：

- imports
- exports
- classes
- functions
- interfaces
- React组件

输出：

```json
{
 "functions":[
   "startTimer",
   "pauseTimer"
 ]
}
```

不要总结代码。

只负责结构化。

---

## 模块四：项目理解器

职责：

判断：

这是：

- React
- Vue
- Flutter
- Expo
- Next.js
- Python

依据：

识别：

```
package.json
```

```
pubspec.yaml
```

```
requirements.txt
```

输出：

```
React Native Expo项目
```

---

## 模块五：AI解释器

目录：

```
src/ai
```

职责：

把结构变成人话。

输入：

```json
{
 "file":"timer.ts",
 "functions":[]
}
```

输出：

```
负责计时逻辑。
```

要求：

必须固定Prompt。

禁止自由发挥。

Prompt原则：

- 不猜测
- 不编造
- 不输出废话
- 用普通人语言

---

## 模块六：Git修改分析

目录：

```
src/git
```

职责：

读取：

```
git diff
```

输出：

修改摘要。

例如：

```
修改：
Button.tsx

影响：

按钮外观。
```

---

## 模块七：缓存系统

目录：

```
src/cache
```

职责：

避免重复调用AI。

缓存：

```
SQLite
```

存储：

- 文件Hash
- AI解释
- 更新时间

文件变化后重新生成。

---

## 模块八：UI展示

布局：

```
左
文件树

中
代码预览

右
解释卡
```

右侧展示：

```
作用

功能

影响范围

最近修改
```

---

# 九、AI协作规范

所有AI必须遵守。

## 原则一

一次只开发一个模块。

禁止一次生成整个项目。

---

## 原则二

每完成一个模块：

必须：

- 自测
- 修复
- 再继续

---

## 原则三

所有代码：

必须：

- TypeScript
- ESLint
- Prettier

---

## 原则四

禁止：

- 假实现
- TODO占位
- 空函数

功能必须可运行。

---

# 十、开发路线

## 第一阶段

目标：

完成扫描。

任务：

- Electron启动
- 选择目录
- 输出树

验收：

能够显示整个项目。

---

## 第二阶段

目标：

识别语言。

验收：

所有常见文件正确分类。

---

## 第三阶段

目标：

AST分析。

验收：

能够提取函数。

---

## 第四阶段

目标：

AI解释。

验收：

点击文件即可看到人话。

---

## 第五阶段

目标：

Git修改解释。

验收：

能够把Diff翻译成人话。

---

# 十一、未来扩展

- 项目知识图谱
- 修改影响预测
- AI风险提示
- 一键生成项目文档
- 自动生成README
- 多模型切换
- 本地LLM支持

---

# 十二、最终验收标准

用户打开任何项目后，应能在30秒内理解整个项目的大致结构。

点击任何文件，应在3秒内看到：

- 它是什么
- 它负责什么
- 它影响哪里
- AI刚修改了什么

整个过程无需阅读代码即可获得全局认知。