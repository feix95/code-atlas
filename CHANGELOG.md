# CHANGELOG

> CodeAtlas 的完整开发历程,一锤一条照实记。新记录往最下面加(「第几锤」编号只活在这儿,commit message 走 Conventional Commits)。

## 进度


- [x] 第一锤:项目骨架(Electron + React + TypeScript)
- [x] 第二锤:目录扫描器(选择文件夹 → 目录树 + 文件统计)
- [x] 第三锤:语言识别器(后缀速查 + 内容嗅探,界面语言分布)
- [x] 第四锤:AST 结构分析(点文件看结构:函数/类/接口/组件/导入导出)
- [x] 第五锤:项目关系分析(谁引用谁 · 影响范围 · 最忙文件排行)
- [x] 第六锤:AI 人话解释(点「用大白话解释」,本地大模型讲这文件是干嘛的)
- [x] 第七锤:Git 修改翻译(谁动了代码,本地大模型用人话讲这次改了啥)
- [x] 第八锤:讲解大扩容(点文件夹讲它管什么 · 新增 Java/Go/C/C++/C#/Rust · 没结构的文件看名字和片段猜 · 报错全换大白话)
- [x] 第九锤:内置本地模型(AI 设置二选一:LM Studio 或 内置模型 · 推理引擎已内置零配置,小白只管「选择模型」· 流式边生成边显示 · 老配置自动搬家)
- [x] 第十锤:全项目体检(bug 猎杀:换模型自动重启 · 崩溃残留的孤儿进程自动收尸 · 流式看门狗防卡死 · 连点文件的竞态防护 · 垃圾审计:应用零日志零残留,崩溃转储自动清理)
- [x] 第十一锤:全树速览(文件夹一进来就自动体检:每个目录/文件后面挂一句大白话标签,纯规则引擎毫秒级 · 文件名字典 + 目录习惯 + 内容统计,认不出就说没认出,绝不编)
- [x] 第十二锤:大文件夹防线(修「选视频文件夹就卡死」:语言嗅探真·只读文件头 4KB,视频/压缩包等二进制后缀零 I/O 放过,超 5MB 不嗅探,全项目嗅探并发限流 32 · 扫描耗时和内存从此与单个文件大小无关)
- [x] 第十三锤:分级扫描(修「选整个 C 盘就卡死」:单次扫描设节点预算,小项目照样一次画完整张图,巨无霸到量就收、没探到的目录挂「点开探一探」,点哪个探哪一层 · C 盘实测 297ms 秒开,顶层永远看得见)
- [x] 第十四锤:分级扫描断链修复(「点开探一探」接回来的子树 relPath 现在带全项目前缀,点文件分析/关系图跳转/嵌套探测恢复正常;子目录自己没探完时照实挂「不完整」,不再装完整)
- [x] 第十五锤:系统禁区防线(修「扫 C 盘弹『文件夹不存在』+ 假『空文件夹』标签」:Windows 锁住的系统文件夹从地图上直接绕开;真撞上锁了的目录/文件,老实说「系统不让看」,绝不谎报「不存在」;模型第一次热身时提示「可能得等一会儿」,不再像卡死)
- [x] 第十六锤:资源管理器式双栏布局(左边目录树、右边全部分析信息,点哪儿看哪儿,再也不用来回滚屏;选中文档/文件夹 AI 自动开讲,不用再点讲解按钮;连点切换时旧的生成自动掐断,模型马上讲下一个 · 灵感来自 inspiration/ui-ref-01/02)
- [x] 第十七锤:可拖分割条(左右栏中间那条线现在能像 VSCode 一样按住左右拖,宽度记在本地,下次打开还是自己调好的样子;双击回默认,键盘方向键也能微调;右栏保底 360px,树再宽也挤不没分析内容)
- [x] 第十八锤:地址栏(顶栏路径从「只能看」变成「能输能贴」:输入或粘贴文件夹路径回车直接开图,不用再弹系统对话框;自动剥掉粘来的首尾引号;路径不存在/填成文件了,说的是清楚的人话,还告诉你该填哪层)
- [x] 第十九锤:说真话(修「点 swapfile.sys 报『文件不存在』」:Windows 独占的系统核心文件现在老实说「被系统占用读不了」,不再谎报不存在吓人;stat/读文件/读片段三处口径统一)
- [x] 第二十锤:任何文件都能分析(图片/音视频/压缩包/数据库不再被「不劳烦模型」挡门外:读文件头魔数认类型,PNG/JPEG/GIF/BMP 连尺寸都认,MP4/ZIP(Word/Excel/JAR)/EXE/SQLite 等十几种,类型+名字+大小当证据让模型讲,开头声明「按类型推测,没看到内容」,绝不编造)
- [x] 第二十一锤:按钮状态语言(修「空路径点前往像坏了」:按钮永不因没填东西变灰禁用,点了才提示缺什么+输入框轻晃聚焦;忙碌中的按钮文字自己变进度;沙漏/禁止光标全面退场 —— UI redesign P0-1,设计语言「人话地图」三原则之一:能说话的按钮不装哑巴)
- [x] 第二十二锤:信号灯体系(红色只留给真错误:全项目信息条分三级 —— 灰=随口一说/琥珀=留个心眼/红=真出事;树的「不完整」从红色警报改琥珀提示,「点开探一探」改主色邀请;讲解卡的推测/认不出归灰,错误才挂红 —— UI redesign P0-2,三原则之一:红色只留给真出事)
- [x] 第二十三锤:等待状态统一(每个区域级等待都换「脉冲点+动词」:绘制地图/解析结构/模型组织人话/翻 git 账本一个口吻;系统关动效自动退成静态点 —— UI redesign P0-3,三原则之二:每个等待都有名字)
- [x] 第二十四锤:设计 token 收拢(间距归 4/8 网格九档、圆角三档+胶囊、字阶六档、阴影两档全进 :root,93 处裸值全部改挂 token,肉眼观感不变 —— UI redesign P1)
- [x] 第二十五锤:键盘焦点与组件状态(全屏 :focus-visible 主色环,Tab 走到哪儿看得见;树行光标统一手型;设置输入框补聚焦描边 —— UI redesign P1)
- [x] 第二十六锤:卡片家族与文案(语言徽章统一胶囊形,五个方角徽章退役;界面文案去助手自称,「哥把地图画给你看」改「马上把地图画给你看」 —— UI redesign P1)
- [x] 第二十七锤:微动效(右栏卡片进场 150ms 上移淡入、树新行 120ms 淡入,全程尊重系统「减少动效」设置 —— UI redesign P2)
- [x] 第二十八锤:空状态与引导(欢迎卡变三步引导:开图→点树→听讲;扫描完成轻提示「地图画好了 N 个文件」4 秒自动退场 —— UI redesign P2)
- [x] 第二十九锤:暗色模式审查(全文件扫硬编码颜色:仅三处白字坐在主色底上,两主题均成立;其余颜色全走语义 token,暗色结构性无漏 —— UI redesign P2)
- [x] 第三十锤:工作台重构 —— 选中与 AI 彻底分家(点文件只出静态详情,结构/关系/Git 全是选中即得;AI 改成「AI 助手」:点按钮或挑预设问题才开跑,六态齐全 —— 未请求/请求中(流式)/取消/完成/失败/重试,requestId+aiCancel 机制原样保留,旧响应绝不盖新文件)
- [x] 第三十一锤:工作台重构 —— 树与详情分层(文件夹「点箭头只展开、点名字只选中」,未扫描目录只有箭头才触发分级扫描;详情区固定头部(面包屑/文件名/语言/Git 徽章)+ 五 Tab(概览/结构/关系/修改建议/AI 对话),不再一屏堆全部卡片;关系跳转保持当前 Tab;长路径省略号+悬停全文;「最忙的文件」改「影响范围最大的文件」并配推荐理由;项目概览新增「建议先看」行动区;文件夹不调 AI 也有家底统计;树加搜索过滤 + 方向键展开收起)
- [x] 第三十二锤:工作台重构 —— 抽屉化与视觉系统(AI 设置、Git 修改改右侧抽屉,盖上不推内容,Escape 可关,关闭后详情原地不动;全量新皮肤:蓝色渐变桌面上浮一扇圆角工作台窗,参照 codeatlas-ui-reference.html 的色板/树行/Tab/卡片层级,按钮 hover/focus/禁用/忙碌四态齐全,亮暗双色;新 IPC 可选 question 参数让预设问题真生效,扫描器/解析器/依赖图零改动)
- [x] 第三十三锤:修「窗外又套一圈假桌面」—— 参考图里的蓝绿渐变是摆拍道具,App 不画;去掉窗内假桌面层,应用边贴边铺满真实窗口
- [x] 第三十四锤:无边框自绘窗口 —— 系统标题栏摘掉(frame:false + 真透明),窗口本体就是圆角悬浮卡,四角以外露出真实桌面;顶部新增 38px 自绘标题栏(整条可拖窗口、双击可最大化),左上角三颗灰点是真按钮:关/最小化/最大化切换,悬停有反馈;最大化贴满屏幕时圆角描边自动收掉;拖边缩放走 Chromium 命中测试(Electron 原生),扫描/解析/AI 一概没动
- [x] 第三十五锤:设置中心 —— 外观自己说了算(「AI 设置」抽屉升级成「设置」:①亮暗三选 —— 跟随系统/白天/黑夜,自动模式实时跟着 Windows 深浅色走;②三套配色预设 —— 晴空蓝(默认)/青碧/丁香紫,点一下整体换装;③主题色+辅助色随便挑,整族配色(悬停/软底/选中/描边/渐变)从色值现场派生,不用记什么色号;④界面大小 80%~140% 分档缩放,走 Chromium 原生缩放(VS Code 同款思路),多种显示器分辨率各自调舒服,下次打开还是老样子)
- [x] 第三十六锤:修「应用活着、窗户隐身」—— 150% 显示缩放的 Windows 上,transparent 透明窗永远画不出头一帧(ready-to-show 不触发,进程在、日志净、就是没窗),退回实底满窗保打开;无边框/自绘标题栏/三灰点/拖边缩放全保留,圆角悬浮等找到稳路子再上
- [x] 第三十六锤补:退透明后窗还是不出来 —— 病根是无边框模式下 ready-to-show 在 4K+150% 缩放屏上永不触发,show:false 的窗就永远藏着;改为随创建直接露窗(show:true,底色同色),电脑操控实测窗已出、标题栏/三灰点/欢迎页渲染正常
- [x] 第三十七锤(体验补完包·模块一):LLM 上下文升级 —— 文件夹/单文件讲解的提示词补喂完整路径 + 通用后缀分布(.exe/.dll/.log 什么文件都数,不再只认编程语言);两套人设明确「完整路径是知名系统目录/软件地盘(Windows、Program Files、AppData)就直接用常识介绍,别装作只能从清单瞎猜」—— 修「解释 C 盘 Program Files 只会说存放各种程序的车轱辘话」;代码项目提示词回归自测不退步
- [x] 第三十八锤(体验补完包·模块A):追问对话框 + 多轮上下文 —— 文件/文件夹详情都能接着追问(「AI 对话」Tab,文件夹也有了输入框);推荐追问按钮(这个能删吗/删了会有什么影响/这是什么软件留下的)点一下填进输入框,改两个字再发;新增追问人设「追问答疑官」:敢给明确倾向不拿「咨询专业人士」打太极、系统关键目录(System32 等)明确劝阻、建议删就带「先移回收站观察几天」的安全网、没把握老实说判断不了;追问必带历史(最近几轮问答)且证据每轮重摆,模型不失忆;历史条数/长度封顶,4096 上下文不撑爆;真模型实测:Aomei 追问能删→「能删+回收站安全网」,System32→「不能删」,连追问记得上一轮
- [x] 第三十九锤(体验补完包·模块B):可选联网识别(默认关)—— AI 设置新增开关「认不出来时允许联网查一下」,默认完全离线;开了也只在讲解自己打「需要联网确认」信号(认出像某软件但说不准是谁)时才查,查的只是文件夹/文件名字、绝不发路径;查询链:中文维基 → 英文维基 → DuckDuckGo 免注册 HTML(跳广告位),5 秒超时、同名内存缓存、查无/断网/改版一律安静回退本地推测不报错;联网请求只在主进程走 Chromium 网络栈(自动跟随系统代理),渲染进程零接触;真模型+真联网实测:Aomei → 修正版点名「AOMEI Partition Assistant 免费硬盘分区管理软件」
- [x] 第三十九锤补:修「联网开关存不住 + 追问让查不查」—— ①主进程保存配置时把 webLookup 字段半路扔了,开关一点保存就弹回原样:补上字段一起落盘;②追问里点名叫联网(联网搜搜/查查/查一下等)之前根本不理:现在开关开着就按名字现查一份公开资料塞进这一轮,追问人设加诚实条款(附了资料必须把对得上的信息讲出来,不许复述文件夹;没附就老实说没查到,绝不装搜过);真模型+真联网实测:食戟之灵文件夹追问「联网搜搜最新信息」→ 回答点名漫画作者+动画各季,并把「貳之皿/餐之皿」对回子文件夹
- [x] 第四十锤:自由对话 + Atlas 小探针 —— 「追问」拆成真正的开放式聊天:新增独立通道 atlas:ai-chat 与独立钩子 useAiChat,文件解释通道彻底剥掉聊天分支,两边不再互相污染;新人设「Atlas 小探针」:问「你是谁」就稳稳报身份,不把每个问题都猜成当前文件;当前选中对象的扫描资料改成「信息挂件」(<context_attachment> 附件,仅供参考、不进对话历史),切换文件/文件夹旧聊天连同在途请求一起清场,概览↔自由对话来回切记录不丢;联网状态全由程序记账(没请求/开关没开/查着/查空/失败六态),边查边播报,消息下挂真实来源标签,模型自述不算数;顶部新增可折叠资料附件卡;CSS+SVG 手搓 Q 版小探针:待命呼吸慢摆、思考摆快眼动、出错变琥珀色,支持系统「减少动态效果」;真模型+真联网实测:「你是谁」→ 稳报身份不猜文件夹;「联网搜一下」→ 中文维基命中,回答点名附田祐斗/佐伯俊并把三季对回子文件夹;闲聊冷知识正常接话
- [x] 第四十一锤(体验补完包·模块四):圆角悬浮窗回归 —— 先把病根钉死:①「系统级 DWM 圆角」(roundedCorners)是 Windows 11(22000+)的 API,本机 Win10 19045 根本不认,抗锯齿 14px 圆角只有透明合成一条路;②「窗隐身」真凶锁定:show 眼巴巴等的 ready-to-show 只在 GPU 磁盘缓存健康时才触发 —— 缓存被另一个实例锁住(实测复现:dev 跑着时再起探针,Gpu Cache Creation failed,ready-to-show 装死)或当年两实例缓存大战污染时永远不来,跟透明不透明无关。方案:transparent 回归 + 露窗链多路抢跑(ready-to-show 快路 / 渲染层双 rAF 首帧信号 / 加载后催帧),3 秒看门狗唯一无条件兜底,窗口绝不永久隐身;CSS 重画悬浮卡:body 留 10px 透明裙边给阴影呼吸,.app 圆角 14px + 描边 + 双层阴影,最大化时裙边/圆角/描边/阴影全收,抽屉遮罩跟着裁圆角;新增 ATLAS_PROBE_DIR 环境变量驱动的验收探针(露窗后整窗截图+记账再退出);1.0 / 1.25 / 1.5(原生 150%)三档缩放全部实测:露窗正常、四角像素 alpha≈全透、中心不透明、截图肉眼可验圆角与布局
- [ ] AI 对话记录 + 追问
- [x] 第四十二锤(体验补完包·模块五):窗控按钮挪右上 + 功能色区分 —— 三颗圆点从左上角搬到右上角,顺序照 Windows 习惯(最小化 → 最大化/还原 → 关闭);关闭单独偏红(警示色,不用 hover 就能认出危险,悬停环也带红),最小化/最大化保持中性灰只留一丝深浅差;悬停变深+外圈浅环的反馈原样保留;标题栏文字居中不受影响;原生 150% 缩放探针截图亲眼验收
- [x] 第四十三锤(体验补完包·模块六):辅助色分配重做 —— 新增派生 token --secondary-deep(辅助色的文字安全档,同一色相按亮暗主题压深/提亮,自定义色实时跟随);辅助色新接三处活:预设问题 chips(文件卡/自由对话/设置里所有推荐问题)、信息类徽章(.badge-blue:语言/git 状态/项目根)、幽灵按钮悬停态(.btn-ghost:hover);加上原有的 logo 渐变、目录分布条、小探针铃铛共六处可见 —— 换算验证:默认皮肤 chips=#1f7e8e 青蓝档,塞 #e0524d 红辅助色后 chips/徽章整族跟红(rgb 173,35,31),语义徽章(已扫描绿)与主按钮(主题蓝)不受影响,层次不乱
- [x] 第四十四锤(体验补完包·模块七):缩放改连续滑条 + 高倍率侧栏比例修复 —— ①复现定根因:内缩放 140% 时视口从 1200 缩到 857px,左栏仍死钉 340px 固定像素不跟 zoomFactor(实测数据在案),文字变大面板不变导致「左栏偏小挤一团」;②修法:localStorage 改存「100% 缩放下的基准宽度」,渲染宽度=基准值×缩放系数,拖分割条按当下系数反算回基准值再存,缩放滑条一动就发 atlas:ui-scale 事件让侧栏实时重夹重算,双击回默认也乘系数;③交互换连续滑条 <input type=range> 80%~180%(步进 5%),± 按钮微调,% 读数居中,主题色渲染;④旧上限 1.4 提到 1.8,越界旧存档从「静默跳回 100%」改为「贴边处理」;⑤实测(最大化 4K 窗):100%/140%/180% 侧栏 340→476→612 精确等比,180% 截图肉眼验:树清晰、比例协调、右栏可用无截断
- [x] 第四十五锤:修「目录概览卡片间距叠在一起」—— 根因照单确认:.metric-grid 只有上边距、.card-text 整个 margin:0,两者贴死;按文档用邻接选择器精确限定,顺手补了两个姊妹场景(实测确认也存在):.card-text + .card-text(「主要语言」行和速览行贴死)、.metric-grid + .two-col(项目概览统计方块和双栏贴死);探针实测间距:方块→文字 12px(--space-3)、两行文字间 8px(--space-2),截图肉眼验收三层分明
- [x] 第四十六锤:排查「设置面板 × 关闭钮热区错位」—— 按文档方法做了基准与组合矩阵实测(系统缩放 1.0/1.25/1.5(原生) × 应用缩放 1.0/1.25/1.4/1.8):①渲染层命中检测(elementFromPoint 5×5 网格 + 中心点)在所有组合下都与视觉盒一致;②透明/不透明窗口差分无差异;③期间发现两件仪器事实:注入点击测试一度「复现偏移」,修正坐标口径后确认是探针自身单位乌龙,不是应用 bug,这条复盘写进记录防止后人再踩;④渲染层能测的范围内无可复现偏移,残余嫌疑在 OS→窗口输入层,应用侧兜底:图标按钮热区 30px→36px(字形视觉不变,line-height 锁 1),命中容差提升 44%;⑤**这条是数值推算+注入实验结论,真鼠标手感没有实测数据 —— 明早小葵真鼠标多缩放过一遍,若仍偏,下一锤上根治方案(改 CSS 缩放替代 webFrame zoom)**
- [x] 第四十七锤:界面缩放换根字号引擎 —— 三个连环 bug(设置面板 × 视觉正对却点不中 / 高缩放下拖边热区比可见框大 / 设置页比窗还大)根子同一个:webFrame.setZoomFactor 是 Chromium 整页缩放,渲染坐标系被整体缩一顿,无边框自绘窗的鼠标判定和视觉框对不上。整引擎换掉,不再打补丁:缩放 = 改根字号(html 的 font-size = 16px × 系数),main.css 244 处尺寸全挂 rem(字号/间距/图标/圆角/栅格列宽/抽屉宽),<3px 发丝线与微位移、窗壳本体(10px 透明裙边/14px 圆角/悬浮阴影)、SVG 内部轴心按用户单位保留物理像素,媒体查询条件按视口 px 原样;侧栏宽度/树缩进改 rem 渲染(rem 值 = 基准宽/16 恒定,根字号一动自动等比,拖割条/键盘/双击回默认的基准值口径不变);滑条改「拖动只预览、松手才应用」,加减按钮仍即点即用;webFrame 引用全项目清零,无半新半旧过渡态。探针矩阵实测(系统 1.0/1.25/1.5 × 应用 1.0/1.8 共六组合,一轮一进程):根字号 16px→28.8px 精确生效;sendInputEvent 在「视觉坐标=输入坐标」下真打命中 —— 设置齿轮开、× 一次点关,六组合全过;壳几何六组合恒定 (10,10,1180,780),可见框不再随缩放漂移;抽屉右缘贴窗不越界;100% 基线截图与换引擎前肉眼无差,150%×180% 截图比例协调无截断。真鼠标手感仍待小葵实测验收
- [x] 第四十八锤:修「设置抽屉戳穿悬浮卡外壳」—— 小葵截图抓包:最外圈是 Windows 真窗口,四周留 10px 透明裙边给阴影呼吸,界面卡片浮在中间;抽屉层却按 inset:0 铺满整扇窗,一开设置白板就从卡片上/下/右各冒出 10px 把壳戳穿,遮罩还把裙边一起罩暗 —— 从圆角壳回归起就带着,此前几轮都在修「点不点得中」,没人量过「抽屉该铺到哪」;第四十七锤探针又拿窗口当参照系,把「白板铺满玻璃」误判成合格(尺子拿错,眼睛是对的)。修法:抽屉层 inset 0→10px 与卡片严丝合缝,遮罩只罩卡片、裙边恢复全透,最大化时回 inset:0 照旧铺满;探针参照系改以 .app 卡片为尺,实测三组合(dsf1.0×scale1.0 / dsf1.0×scale1.8 / dsf1.5×scale1.8):抽屉层矩形与卡片矩形逐边相等 (10,10,1190,790),截图肉眼验裙边全透、白板不出圈
- [x] 第四十九锤(修复包·三):配色系统重做 —— ①预设与自定义分家:AppearancePreset 新增 'custom' 与三预设平级,设置页配色区改成互斥单选(预设三块+「自定义」一块),点预设清空自定义色、点自定义才弹取色器(拿当前生效色当起点不空手),applyAppearance 的 custom 档直接用用户色不跟预设攀亲戚、颜色缺失(老存档坏了)兜底回晴空蓝不炸;②辅助色接管 --line 全局边框分隔线(降饱和+亮度限位成带色相的灰,进 TOKEN_KEYS 统一派生/清理),样式表原值留作晴空蓝兜底;③主题色新接「画布底色」--canvas-tint(亮 3%~6% 饱和/97% 明度、暗低明度,.app 最外层与 .detail/.detail-header 卡片画布染色,.card 等内容卡片保持中性白不动文字对比度)。探针三状态实测:丁香紫 --line=hsl(258) 画布 hsl(256 6% 97%);自定义红主题+青辅助 --line=hsl(175) 跟辅助色、画布 hsl(2 6% 97%) 跟主题色,职责分明;切回默认内联 token 全部清零回落样式表 #d9dcdf,不串色;截图肉眼验紫皮整体协调
- [x] 第五十锤(修复包·一):AI 设置改自动生效 —— 保存按钮退役,跟外观设置一个路数:离散改动(切 Provider、选模型文件、点模型名、联网开关)点一下立刻存,手打文本框(服务地址/模型名/模型路径)失焦才存,不打断打字也不把半截内容写盘;留了一份最近落盘快照,失焦没改动不白写盘;「✅ 已保存」提示保留,只是从点按钮触发改成检测到改动并存成功才冒出来;顺手收掉因此空出来的 .ai-actions 死样式
- [x] 第五十一锤(修复包·二):缩放滑条「松手才应用」—— 主体交互(拖动只挪滑条和 % 读数、松手才真换根字号)已随第四十七锤换引擎时落地;此锤按修复包要求补齐触屏路径 onTouchEnd,鼠标(onPointerUp/onPointerCancel)、键盘(onKeyUp)、失焦(onBlur)、触屏(onTouchEnd)四路收尾全部会真正应用
- [x] 第五十二锤(修复包·四):设置面板改抽屉式收起 + 橡皮筋回弹 —— 左缘竖直居中加收起把手(‹ 小把手,平时低调贴边、靠近向外探头变主题色),点它面板先向外弹一小下、再猛地缩出视野(0.32s ease-in,尊重系统减少动效);× / Escape / 点遮罩保留为立即关闭的备用路;收起动画期间整层不接点击、遮罩先行淡出,动画走完才真关,连点不会二次触发
- [x] 第五十三锤(修复包·五):AI 助手卡加「去追问」—— 文件/文件夹概览的 AI 卡上、「再解释一次」旁边新增「去追问」按钮,点了直接跳到自由对话 Tab,省得鼠标横穿详情区找入口;当前文件/文件夹的扫描资料本来就按现有机制随 FreeChatPanel 的 context 自动带上,不用重新选;按钮在未请求/分析中/完成三态都在,想先跳过去聊也一样能点
- [x] 第五十四锤(修复包·六):自由对话示例问题点了直接发 —— 「你是谁/联网搜一下它是什么/讲点轻松的」三个示例按钮从「填进输入框等手点」改成直接走 chat.send 同一条流程,小探针忙着回上一题时不接新问题;详情页「推荐追问」(AiAssist 的 onPick)按修复包要求原样保留填空式设计,两个组件行为本来就该不一样
- [x] 第五十五锤(修复包·七):窗控圆点画上功能图标 —— 最小化=深灰短横线,最大化=深灰小方框,关闭=白叉压红底;方框随窗口状态变形,贴满屏幕时变双框示意「点这里还原」(加分项做了);图标挂 rem 随缩放一起变大变小,发丝粗细留物理像素,180% 缩放截图肉眼验三颗图标清晰不溢出
- [x] 第五十六锤:圆角贴满窗,悬浮阴影退役(小葵拍板)—— 界面卡片直接铺满整扇窗:去掉 10px 透明裙边和 CSS 悬浮阴影,只在四个角用圆角裁掉透桌面;透明玻璃窗保留(Win10 没系统圆角,透明合成仍是唯一抗锯齿圆角的路),描边留 1px 帮圆角边界定型;抽屉层同步回 inset:0 跟卡片同框圆角裁切;最大化照旧收圆角满屏不漏缝。探针像素实测:窗态四角 alpha=0(圆角透)、上/左边缘与中心 alpha=255(贴满),最大化真命中切换后满屏方角;截图肉眼验窗态/设置抽屉/最大化三态协调
- [x] 第五十七锤:设置界面按效果图重构 —— 右侧抽屉升级为居中设置弹窗(外观与阅读/智能辅助/高级选项三分区 + 左侧导航点击滚动定位、当前工作区卡、汇总卡、页脚);交互逻辑改「暂存 + 预览 + 应用」:所有改动进草稿、界面即时预览(配色/亮暗/缩放当场变),点「应用更改」才落盘,恢复默认/关弹窗(×、Esc、点遮罩)退回上次保存的样子,头部挂未保存药丸、页脚挂同步状态;配色主题升级四张色卡(雾空蓝[默认]/青碧[低对比]/丁香紫[柔和]/自定义[手动调整],自定义点亮才出取色器),界面大小换步进器+滑条+刻度(80%/舒适/180%)并挂实时预览条,AI 来源换双卡分段选择+当前来源说明卡(就绪/未配置如实播报),联网查证换真开关+隐私说明可展开,本地模型连接收进高级折叠面板;⌘ 快捷键符号按小葵要求去除(Win 无此键也不做绑定);preload 新增 previewUiScale(只预览不落盘);探针实测:脏态药丸/页脚提示/应用后 localStorage+AI 配置落盘(configSaves=1)/关窗回退(data-theme 与存档均还原)全过,亮暗双皮截图肉眼验收;顺手修 grid 行高未锁死导致分区内容压住页脚按钮的布局 bug
- [x] 第五十七锤补:配色主题卡对勾压字修复(小葵截图比对)—— 根因是对勾绝对定位在右上角、但文字区不知道它站那儿,卡宽由内容撑出来正好顶到字;修法按确认的方案:所有色卡右侧内边距统一加到 1.5rem 预留角标位(四卡几何一致无跳动),对勾离角微调到 0.375rem;探针实测选中卡文字右缘到对勾左缘净距 6px,截图肉眼验与效果图构图一致
- [x] 第五十八锤:关弹窗先确认,草稿不闷头丢(小葵拍板)—— 根因是快关门设计:点遮罩/×/Esc 走同一个 requestClose 直接丢弃未应用草稿退回存档,改了一半点外面就无声蒸发;修法按确认方案在门口设卡:无草稿照旧秒关,有草稿(外观/缩放/AI 配置任一脏)先弹居中确认小卡(「未应用的更改 / 关闭设置将丢弃这些更改,并恢复为上次保存的状态」,按钮:丢弃并关闭[幽灵款]/继续编辑[主色,默认聚焦]),三条关路(遮罩/×/Esc)同过一道卡,Esc 在确认卡上等于继续编辑不连关设置,确认卡自己的遮罩点了也是继续编辑(默认走安全侧),保存中不响应关窗防半截写盘;探针七步实测:无草稿点遮罩直接关不弹卡 / 有草稿弹卡且设置不关 / 继续编辑留在设置页 / 丢弃并关闭后根字号精确退回存档值 / Esc 先弹卡再按=回编辑 / Esc 弹的卡也能丢弃关闭,全部 PASS;截图肉眼验卡面与设置窗同气(全语义 token,亮暗自适应)
- [x] 第五十九锤:修「最大化后还原图标俩框叠糊」(小葵截图报)—— 根因是双框几何:后框只往外挪 0.1875rem,还没框边厚,俩框几乎同心叠着,且两条边线互相穿过,11px 圆点里糊成一团;修法换经典画法:i 当后面那块原位不动(总足迹与单框一致,不用重找中心),::after 改画底左的前块并实心垫底(填充与圆点同色 #c5c9c8),把后块穿进前块里的线挡掉 —— 后框只探出右上 L 形,即 Windows 还原图标的标准构图;前块 hover/active 跟圆点同档变暗;探针真窗口拍常态+最大化双态截图(顺带补齐:最大化状态靠主进程推 atlas:window-maximized 事件换装,探针照抄推逻辑后 body.is-maximized 才亮),裁剪放大 8 倍肉眼验:最大化态双框错位清晰不再叠糊,常态单框原样
- [x] 第六十锤:一开就是「这台电脑」(小葵拍板四件套)—— ①欢迎卡退役:大 logo/口号/三步引导/引擎版本牌整卡去掉,替代品是盘符列表;②首页=所有盘的根目录:主进程新增 atlas:list-drives,只问 Windows「有哪些盘」(C~Z 逐个 stat 探存在,跳过 A/B 软驱遗物防卡),容量用 statfs 一次系统调用顺手带上(拿不到就只给盘符),不翻任何文件内容秒回;盘符卡片(字母徽标+本地磁盘+剩/共容量)点哪个盘就当打开哪个项目,走现有扫描流程(大文件夹防线/分级扫描原样护航);③logo 变「回项目总览」按钮:钻到多深的子目录,点 logo 一键回项目主页,没开项目自动变灰,hover/focus 有反馈;④版本号搬进设置:高级选项分区新增版本信息行(CodeAtlas 版本走新 IPC atlas:app-version,Electron/Node/Chromium 读 preload 的 process.versions);探针实测七步(假盘符根指临时目录不碰真盘):开屏见盘符视图/卡片容量读数/点盘出工作台/选中文件点 logo 选中清空回总览/设置版本行文案,7/7 全过;截图肉眼验首页与设置版本行排版;顺手清掉 welcome-card/slogan/welcome-steps/engine/brand-mark-big 死样式
- [x] 第六十一锤:自由对话跟滚三件套(小葵拍板)—— 病根是页面结构:整个对话 Tab 一张纸同层滚,输入框靠 sticky 贴纸粘底,新回复往下长没人推卷轴,得手拖;①三段式骨架:详情页 body 在 chat 态改 overflow:hidden 并收掉底部内边距,消息区(chat-messages-wrap + chat-messages)flex:1 自己带滚动条、独占 Tab 下缘到输入行之间的整段,输入框一行回归文档流钉死在底(gap 实测 0px),概览等其他 Tab 滚动方式不变;②粘底自动跟滚:onScroll 记「离底不足 32px 即粘底」,粘底时每批流式增量渲染完把卷轴压到底(瞬时跳,平滑滚跟不上);用户上翻粘底自动解除,程序不抢滚动条;自己发送/点示例问题强制回底;useAiChat 状态机零改动,消息数组每次流式补丁换新引用即天然触发;③向下箭头:不在底部时消息区底部中央浮出圆形小钮(CSS 三角),点击平滑滚回最新(尊重系统减少动效则瞬时),到底自动隐身;上翻期间新内容到达则按钮重挂蹦一下报信;探针真流式实测(假模型分 40 段滴长文):流式中途/结束时均粘底、上翻箭头浮出、点箭头回底且隐身、输入框钉底 gap=0,11/11 全过;截图肉眼验三段式构图
- [x] 第六十二锤:开场态输入栏也钉底(小葵截图报)—— 根因是开场白走的是裸分支:没聊时 .chat-intro 直接躺在 chat-shell 里不占弹性,输入栏跟在开场白屁股后面、面板底下空一截;开聊后消息区才 flex:1 撑满把输入栏顶到底,同一个界面两种状态输入栏高度差半截。修法:开场白并进聊天气泡共用的消息区骨架(chat-messages-wrap + chat-messages),没话时开场白撑满中段、示例问题和输入行照旧钉在面板底,发送后开场白照常退场;探针实测:开场态/开聊态输入栏贴底缝隙均 0px、两态输入栏底同一高度(差 0px)、开场白在消息区骨架内、假模型流式回复到货,6/6 全过;截图肉眼验两态构图一致
- [x] 第六十三锤:AI 干活报告(小葵拍板「审工」)—— 改动页顶层新增报告卡:不读一行代码,「干活审计官」把整轮改动翻成三段式大白话(这轮干了什么[按主题归组≤5条]/账目核对[总数+各类型笔数+点名值得留意的动作]/一句话结论[要不要细看、先看哪个]);账本由主进程现场重取(渲染进程递不进假货),新通道 atlas:git-report 复用 ai-delta 流式管道(生成上限放宽到 900 tokens);签名缓存:改动集(账本+最近提交主题,git log 现取 8 条当背景线索)没变就直接回缓存不重复烧模型,最多留 20 份;提示词清单行数封顶 80 行、超出的按「零碎改动」一笔带过、二进制不报假行数;刷新/换文件/关抽屉把在途生成就地掐掉;账本干净时报告卡退场只留空态,报错原样上屏;自测扩充:真仓库取提交主题/签名稳定性(账本或主题一变签名就变)/提示词封顶与路径契约/假模型服务验「干活审计官」人设与 max_tokens=900,git 链全绿;探针七步实测(假改动账本):有账出卡/流式上屏带游标/回完按钮变「再审一遍」/全文上屏/错误原样展示/账本变干净卡退场,7/7 全过;截图肉眼验报告卡在顶层、账本在底下
- [x] 第六十四锤:git 门搬到你住的那页(小葵截图报)—— 病根是布局失误:git 入口(「N 个文件有改动」推荐卡)只住在项目总览页,而小葵干活住在根文件夹详情页,那页一扇门都没装 —— 明明知道有未提交改动,却满眼找不到入口。修法:文件夹概览新增「git 门」,目录概览卡和 AI 助手卡之间一行可点横条(「🌿 main · 项目里有 N 个文件还没提交 —— 想知道改了什么,点这儿看报告 →」),有未提交改动才亮、账本干净自动退场不骚扰,点击直达 Git 修改抽屉,AI 干活报告就在最上面;App 把 gitInfo/onOpenGit 传进 FolderDetailView→FolderOverview(此前 FolderDetailView 压根没接这两个参数);探针三步实测:有账门亮(带分支名和改动数)/点门开抽屉见报告卡/抽屉里刷新成干净账后门自动退场,3/3 全过;截图肉眼验门的位置与样式
- [x] 第六十五锤:侧边栏(抽屉)系统整体退役(小葵拍板)—— 抽屉有几个老毛病不值得修,干脆整个拆掉,少一层盖层少一套认知;Git 修改抽屉消失,账本和 AI 干活报告搬进右栏原地展开:git 门从「点了开盖层」改成「点了原地展开/再点收起」的展开框(项目总览和文件夹概览两处都住着门,项目总览原来那张「N 个文件有改动」推荐卡功成身退);门行换装:彩色 🌿 emoji 换成 VS Code 源代码管理同款灰描边分支图标(三圆点+岔线,SVG 走 currentColor 随主题),数量徽章沿用 VS Code 蓝底白字圆标挂在图标右下角,展开箭头 180° 翻转(尊重系统减少动效);设置不受影响照旧走设置弹窗;Drawer.tsx 组件、showGit 状态、抽屉 JSX 和整套抽屉 CSS(~130 行)全部清场,文件详情「修改建议」Tab 里两个指向抽屉的按钮改道「回项目总览看全部改动」(总览上门就候着);探针五步实测:项目总览门亮(图标+徽章数对)/点门原地展开账本+报告卡且全程无盖层/再点收起/文件夹概览的门同样能展开/账结清门自动退场,5/5 全过;四件套全绿
- [x] 第六十六锤:修「右下角白块」(小葵截图报)—— 两层病:①全局给滚动条上过色,却漏了 ::-webkit-scrollbar-corner —— 竖向和横向滚动条一交汇,角块就露系统默认的白,暗色主题下是个白疙瘩,补上透明色跟轨道同色;②横向滚动条本身就不该有:右栏被侧栏挤窄时,.two-col 的两张卡(语言分布/建议先看)是网格子项,默认 min-width:auto 撑着最小内容宽不肯缩,把详情区撑出横向滚动 —— 放行 .two-col > * { min-width:0 } 让卡片跟着缩。探针诊断法实测:把侧栏强制拉到 40~66rem 五档,修复前 62rem 起详情区 scrollWidth 331>259 出横向滚动(溢出元凶点名 .sub-card/.rec-row),修复后五档全部无横向溢出;窄右栏截图肉眼验白块消失、卡片窄而不破
- [x] 第六十七锤:认路·「功能在哪」(小葵拍板)—— 项目总览新增功能定位卡:输入一句想知道的事(「程序从哪个文件启动」「配置写在哪个文件」),「项目带路人」照着项目地图指路 —— 主进程把扫描树摊成带完整 relPath 的地图文本(广度优先浅层先画,节点预算 400 封顶并如实注明地图没画全)喂给模型,新通道 atlas:locate-feature 收 (树, 问题) 不重扫不读文件;模型按人设严格回 JSON(每指一个必须带大白话理由,最多 5 个按把握排序),主进程容错解析(剥 markdown 围栏/夹话/反斜杠路径统一成正斜杠)后逐一对照真树点名,编造的地址当场扔掉,全被拦下就老实回「认不出,换个问法」不硬凑;命中渲染成文件卡(路径+语言徽章+理由),点卡片直达文件详情(jumpTo 补上目录命中分支,指中文件夹也能跳);换项目整卡重挂清场,换详情自动掐在途请求;自测扩充:地图摊开(BFS/预算截断/带语言标注)/回复容错解析(围栏/夹话/假把握夹限/去重路径)/防编造过滤(编造扔、目录认、把握排序),AI 链全绿;探针四步实测:功能卡+三示例问法/点示例直接发命中卡带路径理由/点卡直达 a.js 详情/装找不到时老实交代,4/4 全过
- [x] 第六十八锤:修「地图比模型脑子大」(小葵截图报)—— 功能定位在 4096 上下文的本地模型上爆 400:118 个文件的全量地图摊开 4297 tokens,直接把小上下文挤爆。修法三层:①地图改按估算 token 掐预算(新增 estimateTokens:CJK 一字记 1 token、其余 4 字符记 1,刻意保守宁可高估),默认 2200,给人设/问题/回复留足地盘,4k 小模型也装得下;②真撞上「装不下」(按错误特征识别)自动把地图砍到三分之一重试一次,不劳用户再点;③重试还不行才报错,且换成大白话(「把模型服务的上下文调大些再回来问一次」),不再甩生面孔的 400 JSON。自测扩充:150 个带中文摘要文件的树(模拟小葵场景)验证地图加备注不超 token 预算、截断如实注明、CJK 估算口径,AI 链全绿;四件套全绿
- [x] 第六十九锤:AI 层看模型下菜碟(小葵拍板)—— 上下文不再写死:内置模型只留「模型上下文」一个参数,细节控自己外接 LM 去管。①探测:发请求前先问 LM 本人要上下文 —— LM Studio 走 /api/v0/models(已加载上下文优先、标称兜底),llama-server 走 /props(n_ctx),本地 3 秒超时、同模型 5 分钟缓存,探测不到安静回退;②预算公式化:地图 ≈ 上下文×55%、回复上限 ≈ 上下文×20%(下限 600/256、回复封顶 1024),功能定位、干活报告、自由对话、文件讲解、git 翻译全通道统一按真实上下文缩放 —— 4k 模型自动省着用,32k 模型地图全喂,一个公式通吃;③手动档:AI 设置「本地模型连接」里新增「模型上下文」输入框(留空自动探测,探测不到按 4096),填了以用户为准;④保险丝:locate/report 撞上「上下文装不下」类错误自动砍半重试一次(400 时模型一字未吐,流式无重影),重试还不行才给人话错误。自测扩充:LM Studio/llama 两家探测解析(含已加载优先/模型对不上/垃圾回复/过小不可信)、小葵的 400 特征识别不误伤普通错误、4k/2k/离谱输入三档预算公式,AI 链全绿;四件套全绿
- [x] 第七十锤:模型状态栏(小葵拍板)—— 窗口最底下钉一条常驻状态栏,模型热身到哪了随时看得见;别处一个字不动(小葵明确:不加等待文案联动)。①五态:还没叫醒(灰 · 懒加载,没提问不启动,报上次用的模型名 + 文件大小,没选模型/文件失踪给引导话)/热身中(琥珀呼吸点 + 百分比 + 细进度条:引擎 /health 的 503 响应体里本来就带进度,以前被扔了现在捡回来用;服务不报进度就转圈,绝不编数)/就绪(绿 · 报模型名 + 大小,绿线满宽一次退场)/出岔子(红 · 人话消息:程序起不来/加载超时/跑着跑着崩了,下次提问自动重启)/没连上(红 · 外接 LM Studio 没开服务);②取消与卸下:热身中「取消」、就绪后「卸下」,就地解散 llama-server 腾内存,主动叫停不说成故障(「加载取消了:想用的时候再问一句,它会重新热身」);外接 LM Studio 的装卸归它自己管,这边只看状态不给按钮;③管道:builtin.ts 当状态播报员(记账 + 广播 atlas:model-status,轮询 /health 时顺手报进度),外接没法订阅只能低频问 —— 每 10 秒摸一轮 /api/v0/models(老版本回退 /v1/models),设置一保存立刻刷,首帧加载完也刷一轮;渲染层 ModelStatusBar 常驻 .app 底部,首页/项目页都在;④顺手焊两条缝:69 锤的「模型上下文」存取两头都弄丢了(设置里填了白填)—— loadAiConfig/saveAiConfig/保存 handler 三处补齐,而且手动上下文现在直喂引擎启动参数 -c,预算和引擎本尊终于吃同一个数;自测扩充:进度解析不打诳语(0~1 当比例、1~100 当百分数、出范围/垃圾一律 null)、LM Studio 状态映射(loaded/loading/not-loaded/查无此模型/垃圾回复)、contextSize 存取回读;探针五态巡检 18/18 全过(含按钮点击真出活、取消后回「还没叫醒」、截图核对钉的位置),顺手逮住「idle 态误画进度条」修掉;四件套全绿
- [x] 第七十一锤:热身估价进度(小葵验收 70 锤发现「没有百分比也没进度条」)—— 查穿了病根:小葵引擎是 llama.cpp 0.3.0-dev(build 10786),哥翻同版源码证实:加载期间 /health 一律 503 且响应体不带进度字段(旧版的进度口这版重构挪走了),真进度只走内部回调喂给它自家的路由父进程,启动日志也不打印一个百分号 —— HTTP、日志、管子三头都没数,70 锤的「转圈」已是诚实上限;外接 LM Studio 不受影响(它报状态照收);哪天引擎把进度口加回来,70 锤的解析器自动变真数。小葵拍板「估价进度」:①热身耗时小账本 —— userData/model-warmup.json 按模型路径记上次真实热身耗时,就绪那一刻入账,应用自己的小账本不碰项目文件;②估价公式 estimateLoadProgress —— 已用时÷上次耗时,1% 起步、封顶 95%(没真就绪绝不谎报 100),没账/坏账回 null 照旧转圈;③轮询期间每秒播报估价(引擎不监听端口的空档也照走,百分比不卡原地),真进度永远优先于估价,估出来的带 estimated 标记;④界面「热身中 约 35%」,悬停说明「进度按上次热身耗时估的 —— 引擎不报真数,这里不编数」;同一台电脑同一块盘热身耗时相当稳,Windows 复制文件同款思路。自测扩充:估价公式(对半/起步/封顶/没账/坏账/负账/向下取整)与账本解析(按路径记账/没记过/垃圾账本/小账不可信/重热身盖旧账/别家账不动/1 秒下限),AI 链全绿;探针复验「约 N%」/真数不带约字/就绪退场 4/4 全过 + 截图核对;四件套全绿
- [x] 第七十二锤:热身不掐表(小葵拍板)—— 修「模型加载超时(等了两分钟还没就绪)」的冤案:小葵机器实测 32GB 内存 + RTX 5060 Ti 16GB 显存,14.26GB 的 27B 模型整个塞显存绰绰有余;病根是模型住在 2TB 机械硬盘(F 盘实测 Disk1 HDD)上,冷加载要把 14.26GB 从盘里搬进显存,哥掐表实测 90~110 秒才搬了 12.77GB,全程要两分多钟 —— 而 9 锤以来的 120 秒硬超时恰好卡在终点线前把健康的加载掐死,报错还冤枉「模型可能太大,换个小的」。小葵拍板:不掐断,去留交给用户手里的「取消」。改法:①删掉 READY_TIMEOUT_MS 闹钟,就绪等待循环改成「引擎进程活着就一直等」,只有进程真死了才报「进程提前退出」;②五分钟温柔提醒 warmupNudgeMessage(纯函数可测):热身满 5 分钟,状态栏提醒话里说一句「这次热身有点久:大模型第一次要从硬盘整个搬一遍,慢是正常的;不想等就点取消,搬完它自己会说」,半小时也还是这一句,不升级不恐吓;③估价进度照爬(封顶 95 停住 = 还在搬),真就绪那一刻热身耗时照旧入账,下次估价更准。自测扩充:提醒阈值(0/差一秒/整五分钟/半小时四档),AI 链全绿;迷你探针验证「热身中 约 95% + 模型名 + 大小 + 提醒长话 + 取消」同屏不挤(窄窗提醒话截断防溢,悬停看全文)5/5 全过 + 截图核对;四件套全绿
- [x] 第七十三锤:提前量尺(小葵拍板)—— 修「app 不提前把关,出了事才说话还说不准」:71/72 锤查明了病根(机器装不装得下全靠引擎自己拿主意,引擎死了也不分死因),这锤把把关提到「搬之前」。①量尺公式 judgeModelFit(纯函数可测):选模型那一刻就拿模型块头比机器尺寸 —— 有显存数据时 ≤90% 显存 = 绿「装得下,整个进显卡,跑得动」、挤到「显存+一半内存」= 黄「有点挤,能跑但会慢」、再超 = 红「这台机器装不下,建议换 N GB 以下的模型」;问不到显存(A/N 卡)就只看内存:五成宽裕绿灯、七成线内黄灯、再超红灯。②机器家底探测 queryMachineSpec:内存走 os.totalmem,显存走 nvidia-smi(纯函数 parseNvidiaSmi 抠「名, 16311 MiB」),问不到老实回 null 不硬编;同一进程只问一遍,app 启动就预热,验尸时张口就来。③设置页量尺灯:模型文件框一变(选的、手打的都算)就问主进程 atlas:model-fit-check,框下当场亮灯 —— 绿✓装得下/黄⚠有点挤/红✕装不下,路径清空灯灭;文件不存在单独说「路径找不到文件」。④引擎死因验尸 autopsyExitMessage(纯函数可测):启动就死(EngineExitError 带退出码)按量尺分流 —— 量尺红灯 = 「模型在这台机器上装不下,引擎一启动就撑死了」+ 数字建议;机器装得下但手动上下文填到 32768 以上 = 点名「设置里模型上下文填得太大(当前 X),清空它回自动探测」;都不沾 = 才归到「文件损坏或被占用」。自测扩充:nvidia-smi 解析(合法/垃圾/N-A)、量尺三灯(小葵实机 14.26GB vs 16GB 显存 = 绿灯实锤、黄灯、红灯、无显存三档)、验尸分流(撑死优先/点上下文名/文件坏兜底/不许再冤枉模型大),AI 链全绿;探针走真设置页 4/4(打字出红灯带建议/换好模型变绿灯/清空灯灭);四件套全绿
- [x] 第七十四锤:底栏改版(小葵 P 稿直接照抄)—— 状态栏从「右边一小坨」改成通栏:左边住模型状态本体(状态点 + 内置/外接标 + 热身中/就绪等状态话 + 取消/卸下按钮,小葵点名按钮也搬左边),右边住模型数据(模型名 + 文件大小),中间弹性带留给提醒/出岔子的话(空间不够自动截断,悬停看全文);进度条从「芯片底下的小线」升级为拉通整条底栏、按底栏全长当最大,前进端点加一颗小发光(currentColor 跟状态走:热身主色、就绪变绿、随退场动画一起隐去;转圈扫描档也带光);模型名悬停区加宽到 24rem。结构拆成 model-left / model-message / model-right 三段 flex,右组 margin-left:auto 兜底把数据推到最右。探针复验 7/7(空闲布局/无进度线/热身 42% 文案取消在左/进度条贴左边缘拉通/宽度准/就绪卸下加满宽绿线/提醒话与取消与右侧数据同屏)+ 截图核对;四件套全绿
- [x] 第七十五锤:热身账本滚动三条(小葵拍板)—— 估价基准从「只记最新一次」改成「最近 3 次成功热身的均值」(第七十一锤的老账自动兼容:单个数读进来当一次历史,不删档不迁移)。为什么:同为成功加载,开机第一次是冷读(硬盘现搬,慢)、之后走系统缓存热读(快好几倍),单样本被「上一次运气」绑架 —— 冷读后估价爬太慢、热读后条子提前顶到 95% 干等;滚动三条把坏运气摊到三分之一话语权,且始终跟着最近走,换盘挪模型三轮就淡出去。实现:①parseWarmupSamples(纯函数可测)—— 老格式单个数自动转一次历史、脏账(<1 秒/非数)剔除、只认最近 3 条、全脏回 null;②averageWarmup(纯函数)—— 样本均值,没样本回 null 不硬估;③nextWarmupStore —— 入一笔留三笔,满三条最老的滚出去,别家的账不动,1 秒下限保留;④估价与提醒逻辑零改动(封顶 95 的诚实底线不变),只是基准从单样本换成均值。账本文件依旧只有 userData/model-warmup.json 一个,几 KB 量级,不碰项目目录。自测扩充:数组账读回/老格式兼容/没记过/垃圾账本/脏账剔除/超长只认末三条/新账入列旧账保留/别家不动/满三滚动/1 秒下限/均值(空/单样本/一冷两热混算/四舍五入)十一组断言,AI 链全绿;四件套全绿
- [x] 第七十六锤:配色改版 —— 遮罩摘除 + 明度安全带(小葵拍板:画布雾保留、明度照饱和度的样子限范围)—— 起因:小葵换绿色自定义主题色后,发现绿雾跟中性卡片融合不好、对话框跟外面像两个世界;查明根因是「整面染色」的遮罩手法 + 派生明度档是当年按默认蓝调的。三刀:①遮罩摘除 —— --accent-soft(气泡/参考卡/提示底)和 --selected-bg(树选中行底)不再跟主题色染色,回归中性面板(暗 #2b333a/#2e3841,亮 #eef0f2/#dfe4e8),样式表默认值与自定义内联两条路一起改,三套预设行为一致;主题色从此只上动作件(按钮/激活页签/logo/进度线)+ 细边(--accent-line 保留色相描边)+ 树选中行左侧那根主题色细条(原本就有,正好撑住选中感);②明度还给用户 —— --accent 明度从写死档改成安全带:暗色 55~85(原 62~80)、亮色 25~62(原 28~58),带内完全听用户选的颜色,出带才夹(带子就是按钮上固定字色的可读边界:暗色按钮压深字、亮色按钮压白字);hover 依旧派生深一档当交互反馈;③画布底的雾保留 —— --canvas-tint 照旧跟主题色染色(暗 L12 低饱和/亮 L97),底色氛围不掐。探针 9/9 全过:自定义绿(#7dba32)色相保留+明度带内照用/soft 与 selected 回中性/画布雾仍跟主题色染/近黑绿(#0f1f0f)明度兜到 55/亮色近白绿(#e8ffe8)兜到 62/亮色遮罩中性/默认皮肤同样中性化,截图核对融合感(绿只在按钮与点缀,面板全中性);四件套全绿
- [x] 第七十七锤:画布雾转中性(小葵拍板「雾转中性(彻底)」)—— 76 锤验收后小葵又截图「还是有啊」:聊天区、底栏、Tab 行旁那三块绿全是同一层东西 —— 画布雾(--canvas-tint 跟主题色染,右栏底/底栏底都用它),上一锤拍板保留的雾在单片看是氛围、铺在聊天区/底栏这种大片空白上就成了罩子,加上设置页还有两处漏网(提示条 cfg-callout 掺 5%、当前配置条 cfg-summary 掺 4% 主题色)。小葵三选一拍了最彻底的:①画布不再跟主题色 —— appearance.ts 两个分支删掉 --canvas-tint 内联写入、TOKEN_KEYS 摘除,画布回归样式表固定中性底(暗 hsl(206 13% 13%)/亮 hsl(207 5% 97%),与中性面板同一族蓝灰),换任何自定义颜色画布纹丝不动;②两处漏网小条回中性面板(var(--surface-soft)),识别靠原有的主题色细边;③按钮投影、聚焦光环里的主题色保留 —— 那是按下/聚焦的交互反馈,不是遮罩。至此配色规矩收敛成一句话:底色永远中性,主题色只活在动作件、细边和交互反馈上。探针 4/4 全过(绿主题画布仍中性/无内联覆盖/主题色照常上按钮/换紫主题画布纹丝不动);四件套全绿
- [x] 第七十八锤:窗口尺寸记事本 + 设置弹窗帽子修偏(小葵报「每次开窗都默认大小,设置的保存按钮被截半截」)—— 病根一(弹窗被截):探针在本机按三场景量出真相 —— 默认缩放下弹窗其实装得下;小葵的「界面大小」停留在约 130%,.cfg-window 那条 rem 写的 min-height:36rem 会跟着根字号一起长大,反过头盖过 height:min(47.25rem,100%) 这顶视口帽子(CSS 里 min-height 永远赢),弹窗被撑到 1037px 高、页脚按钮挤出窗外(探针 B 场景抓现行:1.8 缩放时弹窗 bottom 1087 > 窗高 800)。修法:摘掉 min-height,帽子永远生效,矮窗下内容交给 .cfg-scroll 内滚(头部/页脚是 flex 固定行永远可见);探针复验 A 默认缩放@1200×800 / B 缩放 1.8@1200×800 / C 缩放 1@最小 960×640 全过,应用按钮全程完整。病根二(每次都默认大小,小葵点的菜):窗口尺寸记事本 —— 新增 src/main/window-state.ts,createWindow 开窗前读 userData/window-state.json,上回把窗拉到多大、搁在哪儿这回照旧;最大化单独记一票(最大化时 getBounds 是铺满屏的假尺寸,不能当正常尺寸记),还原时先落回存档的正常大小再 maximize;拖动/缩放/挪位 debounce 半秒记一笔,关窗那一刻清表补写;存档过两道安检(纯函数可测):parseWindowState 只认干净数字、块头不小于窗口下限(960×640);placeWindowBox 把旧存档贴着「现在接着的屏幕」夹紧 —— 挑交叠最大的屏当家,完全出屏/只露窄边(横 <120px 或竖 <80px)就拽回屏内,块头超屏夹到屏大,换显示器/改分辨率也不把窗送出屏外;读写失败一律安静走默认,锦上添花不添乱。验收:真机双跑 —— 首跑优雅关窗,账本如实写下默认居中位(680,300,1200×800);手植一笔 1500×950 @ (180,120) 再开,落位分毫不差(高度 +2px 是 Win32 给无边框窗补的隐形缩放边);(900,200) 同样原样复位;存档 maximized:true 再开,开窗即最大化。自测新套件 selftest-window 十六条断言全绿(parse 垃圾一概不认/小数取整/下限夹紧;place 原样落位/超大夹紧/出屏拽回/窄边拽回/左屏记忆不被主屏抢/半悬保位/竖窄边拽回/无屏兜底;IO 回环/垃圾档不抛),npm test 链挂上 test:window;四件套全绿
- [x] 第七十九锤:自定义配色放开(小葵拍板「色相给满、饱和度给满、明度不分亮暗主题都给 5~95」+ 新攒条规矩第一条)—— 起因:小葵再试自定义调色还是觉得束手束脚(旧规矩:饱和度夹 30~85、明度按亮暗主题分带 55~85/25~62,挑深色被抬亮、挑艳色被夹灰)。三刀:①自定义档彻底放开 —— appearance.ts 派生公式色相、饱和度给满(0~100),明度亮暗同带 5~95,极深极浅都听用户的,hover 依旧派生深一档当交互反馈、夹在同一条带内;②字色安全从「明度带」改嫁「对比度」—— 原来那条明度带保的就是按钮上固定字色可读,现在新增 --accent-ink 令牌:pickAccentInk(纯函数,按 WCAG 相对亮度比深墨 #10222e 与纯白谁对比高听谁的,阈值卡在打平点偏白一档),六处实心主题色件(主按钮/logo 徽标/Git 角标/预设问题 chip/模型路径选择钮/设置应用钮)字色统一走 var(--accent-ink),四处暗色主题硬编码 override 退场,样式表两套主题补默认值(原装预设不写内联的老路不受影响);③预设档按来源分家 —— 青碧/丁香紫是配好的成套皮肤,里子仍走原配档(明度带照旧),长相一分不变;唯一一次字色漂移:暗色丁香紫按钮字由深翻白(对比 3.4 → 4.8,更清楚),探针与自测都钉了这一条。原生取色控件本就全范围,UI 侧无限位,自由直达。自测新套件 selftest-appearance 七条断言(黑白灰亮度/亮色三预设照旧白字/暗色雾空蓝青碧照旧深字/丁香紫翻白明算账/极深压白极浅压深/正红白字正黄深字/小葵的绿带内不夹)挂进 npm test;探针 14/14 全过(正红饱和 100 不夹 85/近黑明度 5 不再抬 55/正黄亮色翻深字/近白明度 95 不再压 62/青碧丁香紫暗色配档不变/默认预设内联不写);四件套全绿。本锤起按新攒条规矩办:琐碎改动不单独 commit/push,攒满 5 条一次落地
- [x] 第八十锤:logo 变退路(小葵拍板「点左上角 logo 就回到这台电脑页面」)—— 从前 logo 只会「回项目总览」,项目一开就没有任何路能退回选盘首页,想换项目只能再开一个。改法:goHome 重写为真正的「回家」—— 清掉当前项目全套状态(地图/选中/关系图/git 账/分级扫描脚手架/报错),退回「这台电脑」选盘页;上次开的路径特意留在输入框里,想回这个项目点「前往」就行;项目没开时 logo 照旧变灰(本来就在家),扫描中也点不动(防中途退出撞上扫描回包的怪状态);悬停提示从「回到项目总览」改「回到这台电脑」。探针 5/5 全过(开机在首页 logo 灰/粘路径开假项目 logo 活/点 logo 回首页侧栏收/logo 归位变灰/路径留在输入框回得来);四件套全绿;攒条第 2 条,暂不 commit/push
- [x] 第八十一锤:主页认识主人(小葵四选:最近打开 + 盘符真话 + 回家重列 + 列盘加固)—— 「这台电脑」从只报盘符的门,升级成认识主人的门童:①最近打开的项目 —— 新增 renderer/recents.ts(localStorage 小账本,纯函数 parse/next/remove 可测):画成一张图才算「打开过」,记 路径+展示名+时刻,同路径不分大小写挤旧顶新,最多 8 条从队尾滚;首页盘符上方一排「最近打开」小卡,点一下直接重新画图,悬停 ✕ 删记录(只删记录不碰文件夹,触屏/键盘用户 ✕ 常显),时间话术「今天 14:30 / 昨天 / 9月1日」(formatRecentTime 纯函数可测);②盘符卡片说真话 —— DriveInfo 扩 label/kind,新增 main/drive-meta.ts 借 PowerShell Get-CimInstance 一次问齐卷标和来路(固定硬盘/U 盘或移动硬盘/网络盘/光驱),失败/超时/垃圾输出一律回空、照旧叫「本地磁盘」,5 秒小缓存;有卷标显卷标、没卷标按类型叫,类型注脚跟在容量前;容量画成一根小容量条(像手机存储),剩不到一成转琥珀;③回家重列盘符 —— 盘符列表从开机一次性改成「退回首页就重列」(含 logo 回家),app 开着时插拔的 U 盘回家永远见得到,重列期间老列表照摆不闪空;④列盘加固 —— 24 个盘符从顺序问改成一起问、单盘 2.5 秒不答当缺席(掉线的网络映射盘拖不住整列),慢半拍的输家就地安静不许变未处理拒绝。自测新套件 selftest-recents 六条断言全绿挂链;探针 10/10 全过(卷标+类型注脚/容量条 80%/快满转琥珀/无容量不画条/最近出现·项目名对/时间话术/回家重列盘符/最近卡片重开项目/✕ 删记录/刷新后存档还在);真机实测 PowerShell 问卷标 671ms、缓存二连 0ms;四件套全绿。攒条第 1、2 条(79 锤配色放开、80 锤 logo 退路)与本锤同文件纠缠,按攒条规矩随本锤一并落地,一并 push
- [x] 第八十二锤:盘卡片不认卷标(小葵拍板「就大写的 C、D、E、F、G 这样直白点」)—— 81 锤把卷标(ssd/software/save/KESU)上墙,小葵看着别扭:盘就认大写字母,卷标下架。盘卡片标题回到类型话术(固定硬盘/U 盘或移动硬盘/网络盘/光驱),注脚回纯容量,大字母 chip 当家;DriveInfo 摘掉 label 字段,drive-meta.ts 瘦身为只问类型(queryDriveMeta → queryDriveKinds,PowerShell 查询少选一列 VolumeName,解析/缓存/回退逻辑不变),类型和容量条不受影响。攒条第 1 条:四件套全绿,探针 4/4(卷标不上墙/类型话术/网络盘/容量条照常),暂不 commit/push
- [x] 第八十三锤:后退 / 前进(小葵点名「跟刷新放一起」)—— 顶栏在「打开项目」和「刷新」之间添两颗 ← →:浏览过的位置(首页、开过的项目、点过的文件/文件夹)排成一条线,按钮在线上挪游标,到两端自己变灰。规矩全按浏览器惯例:连点同一个地方不记第二笔;后退之后再点新地方,身后的前进线剪掉;同路径的刷新不算搬家不记新站;换项目的站重扫那张图再恢复选中,节点没了(重扫后不存在)就老实清空选中,不硬猜。新增 renderer/navHistory.ts 纯函数线轴(pushNavLocation 去重/剪线/满 60 站从最老请出去、stepNavIndex 游标夹边、sameLocation 三样对上才算原地),App 里记录钩子挂在开项目/选文件/选文件夹/logo 回家四个动作上,后退前进跳转途中有铃铛(navTravelingRef)拦着,恢复旧位置引发的一串自动选中不再记新账;scanPath 改为把扫成的图递还给调用方(恢复选中要在新树上找人),扫砸了回 null。自测新套件 selftest-nav 五条断言全绿挂链;探针 12/12 全过(开机两端灰/开项目后退活/选 a.ts 前进灰/后退收详情/再后退回首页/前进两步原样走回 a.ts/刷新不记新站/刷新站后退前进线活/中间改选 b.ts 前进线剪掉);四件套全绿。攒条第 2 条,暂不 commit/push
- [x] 第八十二锤:盘卡片不认卷标(小葵拍板「就大写的 C、D、E、F、G 这样直白点」)—— 81 锤把卷标(ssd/software/save/KESU)上墙,小葵看着别扭。盘卡片标题回类型话术(固定硬盘/U 盘或移动硬盘/网络盘/光驱),注脚回纯容量,大字母 chip 当家;DriveInfo 摘掉 label 字段,drive-meta.ts 瘦身为只问类型(PowerShell 查询少选一列 VolumeName,解析/缓存/回退不变)。探针 4/4(卷标不上墙/类型话术/网络盘/容量条照常);攒条随 84 锤一并落地
- [x] 第八十三锤:后退 / 前进(小葵点名「跟刷新放一起」)—— 顶栏在「打开项目」和「刷新」之间添 ← →:浏览过的位置(首页/开过的项目/点过的文件文件夹)排成一条线,按钮在线上挪游标,到两端自己变灰。规矩按浏览器惯例:连点同地不记二笔;后退后点新的,前进线剪掉;同路径刷新不记新站;换项目的站重扫那张图再恢复选中,节点没了老实清空,不硬猜。新增 renderer/navHistory.ts 纯函数线轴(pushNavLocation 去重/剪线/满 60 站从最老请出、stepNavIndex 游标夹边、sameLocation),记录钩子挂开项目/选文件/选文件夹/logo 回家四个动作,后退前进途中有铃铛拦着自动选中不再记新账;scanPath 把扫成的图递还调用方。selftest-nav 五条全绿挂链;探针 12/12(两端灰/后退活/前进灰/后退收详情/回首页/前进两步原样走回/刷新不记账/中间改选剪线);攒条随 84 锤一并落地
- [x] 第八十四锤:忙态 + token 计数上桌(鲁棒性实测后的 A+B 方案,小葵拍板)—— 起因:小葵开 C 盘大项目提问,状态栏「就绪」却报「模型响应超时,可能模型还在加载,或太大跑不动」。哥拿假引擎把 AI 客户端按在案板上揉了九项,查实:状态栏对「忙/卡住」全盲(就绪 ≠ 闲着)、提示词上万 token 时引擎预处理超 30 秒被 2 分钟看门狗掐死、断线会扔掉已吐的半截回答、空流冤枉「没加载成功」。本锤先治头两条:①状态栏新「忙」态(主色呼吸点)—— 每个模型请求的必经口 resolveChatTargetOrError 过关即进「忙」,收工(explainWithCancel/自由对话 finally 的活账本清零才)回「就绪」,嵌套讲解中途不闪;外接 LM Studio 用最近一轮探测照办,引擎热身时「忙」不抢加载频道的话。②token 计数摆上桌 —— 内置引擎请求带 timings_per_token + stream_options(include_usage),流里每帧捎「读材料中 · 已读 N tokens」/「已吐 M tokens · 速度 tokens/s」:预处理那段最难受的静默从此有数;LLM 前的聊天气泡下实时小灰字,答完换「本次 · 读 x · 吐 y tokens · 速度」尾注;引擎不报就只说「在干活」,绝不编数;外接不塞旗子行为一分不变(引擎自愿报的账照收)。新增 shared/aiText.ts 话术格式化(状态栏/气泡共用一张嘴),extractStreamStats 纯函数解析(垃圾/缺数回 null);stats 帧按字段合并(usage 收尾帧只带 token 数,不许把上一帧的吐字速度冲掉——探针抓过现行)。自测扩充六条(timings 帧/usage 帧/垃圾回 null/两段话术/尾注省段),探针 6/6(内置带旗读材料 prompt_n/吐字计数与速度继承/usage 收尾/外接无旗不受影响);四件套全绿。攒条 82/83 与本锤同文件纠缠,按 CLAUDE.md 纠缠条款一并落地
- [x] 第八十五锤:超时话术按现场分流 + 半截必留(鲁棒性实测后的 C+D 方案,小葵拍板「修」)—— 84 锤前查实的四个洞,这锤清掉剩下三个:①看门狗重修 —— 老流式看门狗用 controller.abort 掐流,实测根本掐不进 Node fetch 的读队列(形同虚设);真出事的预处理静默(首帧之前)反而只有 30 秒耐心,小葵的冤案就是它俩叠出来的。新看守搬进 sseEvents 自己身上:读流 Promise 与「没动静铃」赛跑,首帧前给足 2 分钟(大提示词预处理整段静默是正常的),开始吐字后 30 秒无帧才算真卡住(StreamIdleError);用户取消也插了铃保底,不再赌 abort 的心情。②半截必留 —— 以前断线会把已经吐出来的半截回答全扔掉还报「连不上」(超时和取消倒是留的),标准不一致;现在掐断/卡住/断线一律保住到手部分,注脚按现场给:断线「连接断了,上面是已经生成的部分」/掐断「等太久被掐断了」/卡住「模型可能卡住了」,用户自己停的不加注(界面有专门话术)。③话术修口 —— 空手超时不再说「可能模型还在加载,或太大跑不动」(模型明明就绪,冤枉),改说「等了很久没等到第一个字:材料大预处理就慢……不想等就取消」;空流(连上了一个字没回)改口「上下文可能塞得太满:清点参考材料」;400 上下文塞满(isContextOverflow 认得出 llama 的英文原文)翻译成「材料塞不下模型的脑容量了……或去设置调大模型上下文」,翻不动的其他 HTTP 错误照旧透传原文。自测扩充:halfNote 三注脚/timeoutText 不再冤枉加载/friendlyHttpError 小葵的 400 认得出·500/404 透传,老断言(连不上话术)贴新词;鲁棒性案板复测 5/5(6 秒静默后开口不掐/断线保半截带注脚/400 说人话/非流式照常带尾账/取消说取消);四件套全绿
- [x] 第八十六锤:量尺补课 —— 上下文缓存算进去(鲁棒性实测后的 E 方案)—— 73 锤的量尺只比权重块头:14.26GB vs 16GB 显存亮绿灯,但上下文缓存一占就溢出、预处理慢吞吞(小葵 C 盘大项目提问慢的深层原因),绿灯说得太大方。改法:①新增 estimateKvBytes(纯函数可测)—— 架构细节从文件块头拿不到,按模型大小分档粗估每 token 缓存开销(≥8GB 按 64 层大模型 256KB/token、4~8GB 减半、更小再减半),宁可粗、不可无,话里标明「估」;②显存分支的量尺改成「权重 + 上下文缓存」合起来比:超九成线从绿翻黄,黄灯细节点名「读大材料时会明显变慢;把模型上下文调小能快回来」—— 把「为什么吐字慢」讲在前面,还给了出口;红灯的换卡建议同步扣掉缓存占用;纯内存跑的分支不估缓存(内存机型本来就慢,缓存改变不了结论,不拿估的数字吓人);③两处调用喂上下文:设置页量尺灯读配置里的「模型上下文」(手动填了按手动的,没填按引擎实跑的 4096),引擎验尸(启动就死)拿本次启动用的上下文。自测更新:小葵的案子 14.26GB vs 16GB 从绿翻黄(黄灯点名上下文缓存+读大材料慢)、上下文旋钮断言(调到 512 同块头翻回绿灯)、estimateKvBytes 四档断言;四件套全绿

- [x] 第八十七锤:Developer 日志窗口(小葵点名:像 LM Studio 那样,把模型后台信息亮出来看)—— 引擎的原话以前被 stdio:ignore 整个扔掉,后台在干嘛全靠猜;这锤开一扇窗全透明:①纯日志账本 src/shared/devlog.ts(不依赖 electron,AI 模块/主进程/自测共用一本账)—— 环形缓冲 2000 条,单行超 600 字掐中留头尾,llama.cpp 进度条的回车刷屏按行拆开,进程退出即清不落盘;②引擎原话接入 —— llama-server 的 stdout/stderr 从丢弃改成接管,模型结构/显存分配/加载耗时/报错一行行进账,启动命令(引擎路径/模型/上下文/端口)、就绪耗时、退出码、收尸孤儿、端口被占也都记一笔;③请求报账 —— 所有模型请求都过 explainWithMessages 这道门,起止各记一条:提问只记元数据(地址/模型/消息条数/提示词字数/token 上限/是否流式),完成记输出字数 + token 账 + 耗时,失败记人话原因 —— 用户问了什么、材料里有什么,一个字不落账(隐私线);④独立日志窗口 —— frameless 透明小窗(与主窗同壳同露窗三保险链:ready-to-show + 首帧信号按 sender 认窗 + 3 秒看门狗),渲染层复用同一入口靠 ?view=devlogs 分流;窗口端同样封顶 2000 条防长胖;开窗先拉旧账再订阅新账,「跟到底」自动滚动(用户上翻自动让位)、来源过滤片(全部/引擎/请求/应用)、文字筛、复制全部、清空;主窗关闭时日志窗一起带走,应用照常退出;⑤两个常设入口:模型状态栏右端「日志」小钮 + 设置弹窗高级选项页「打开后台日志」(不用展开面板就够得着);⑥实测一锤定音:暗色主题下头部写死浅渐变把浅字糊掉(探针 computed style 抓到),改主题 token;探针 6/6(渲染成型/新账实时进账/文字筛/来源片/清空空态/关窗线);自测新套件 selftest-devlog 9 断言(按行拆/掐中留尾/环形滚动/编号单调/拷贝不可篡改/监听广播注销/时间补零/来源标签);npm test 16 套件全绿

- [x] 第八十八锤:修「模型明明跑着,底栏却说还没叫醒」(小葵截图报修,grok 顾问参与会诊)—— Developer 日志窗开着时,主窗底栏冻死在「内置 · 还没叫醒」、进度条不出现,但日志窗里引擎原话和「回答完成」记账跑得欢。哥拿代码验尸:病根不在状态机,在广播员 —— broadcastModelStatus 只往 BrowserWindow.getAllWindows()[0] 一个窗发,单窗年代 [0] 永远是主窗,87 锤开了日志窗后「[0]」指向后开的日志窗(探针实测:双窗在场,旧写法主窗收 0 条、日志窗收 1 条),模型状态广播全喂给了没人听的日志窗,主窗底栏只剩开机时问的那一口。改法:①广播员挨个窗都发(和 87 锤日志广播员同款);②同类隐患顺手清 —— 「选文件夹」「选模型文件」两个系统弹窗也从抓「[0]」改成认准来叫它的那个窗,日志窗开着时弹窗不再挂错门。grok 顾问的四条建议采纳一条方向、驳三条:「refreshModelStatus 不管内置」是设计不是病(atlas:model-status-get 本来就读播报员手里的活账),「busy 被 idle 吞掉卡死」对不上代码(内置流程必先 loading 后 ready,顺序保证 busy 不会被吞),渲染层 10 秒轮询是贴膏药掩盖真病,都不取。探针 1/3 断言组(旧写法喂错窗/新写法两窗全收);四件套 16 套件全绿

- [x] 第八十九锤:修「上下文一输入就变 512,大数输不进、删也删不掉」(小葵问逻辑问出了病)—— 设置页「模型上下文」是个受控输入框,老写法把夹紧门卫放在了每敲一个键的瞬间(敲完立刻 max(512, min(1048576, 数)) 再写回框):敲一个 8 当场变 512;框里已是 512,新敲的数字只会接在屁股后面,想要的数永远拼不出来;退格删一个字变 51,又小于 512,当场弹回 512,一个字一个字删永远出不去 —— 门卫规矩没错,站错了岗,跟手抢键盘。改法:打字和落账分家 —— ①打字草稿归框管(纯字符串,只挡非数字,大小不拦,8 就是 8,删空就是空);②落账挪到失焦那一刻(数字归到 512~1048576 里最近的合法数,框里当场改字眼见为实;空 = 交回自动探测);③点「应用更改」时再兜一道底(框里还有没失焦的字也夹进合法范围才落盘);④存档换人的四个时刻(初次读档/恢复默认/保存回写)才回填框,自己打的中间态不被动;⑤没失焦的草稿也算未保存更改,打完字直接关窗照样弹「确认丢弃」;⑥帮助文案把范围和规矩写明白。原本三道门卫(界面敲键/主进程存档低于 512 当没填/引擎启动前再夹)互相重复,现在界面这道终于不碍事。探针 7/7 走真设置页(敲 8 框里是 8/拼出 8192/失焦不动/51 失焦归 512/删空/16384 真落盘/清空落盘回自动探测,临时户头不碰真配置);四件套 16 套件全绿

- [x] 第九十锤(试做即回收):文件树右栏大小账 —— 文件报自身大小、文件夹报名下总和(残账宁缺不亮假数),真应用探针 12/12 全过;小葵试用后拍板不要这个功能,代码已整体 revert 回收(文件/文件夹大小一概不显示),锤号 90 烧在这条回收凭据上,往后续锤不跳号

- [x] 第九十一锤:全树速览准头修补(小葵逐句问「这些解释准确吗」问出的三处病)—— ①字典缝隙补上:光杆 tsconfig.json 掉在字典外只拿到泛泛的「数据/配置小账本」,现在配得上「TypeScript 的尺子」;.prettierrc 全家(光杆/.json/.yml/.toml/.cjs…)以前只有 prettier.config.* 有名分,现在都归「排版规矩」;②事实压过绰号:目录检查顺序倒了过来 —— 被锁的说「系统不让看」、真探空的说「空文件夹」,都排到名字字典前头,空着的 config 文件夹不再对着空抽屉喊「配置间:开关都集中在这」;名字标签照旧给有内容的目录当绰号,未探目录名字认得出的保留标签的老规矩不动;③残账说「至少」:分级扫描截断后数出来的家底(装着 N 个文件/资料间 N 份/分类抽屉 N 格/自测工具箱 N 个脚本)一律改口「至少 N」,不许把下限报成总数,和九十锤立的「残账宁缺不亮假数」一条心;自测新套件 4 断言(光杆 tsconfig/.prettierrc 两代/空 config 说空/截断说至少),npm test 16 套件全绿

- [x] 第九十二锤:一句话解释按《词条撰写执行规则》整体重写(小葵带话题去外部 AI 会诊,拿回一套分档规矩,全文存 ref/词条撰写执行规则.md)—— 核心思想:界面已有图标+文件名+类型小标+数量胶囊,这句话只许补「看不出来但新手需要」的信息,按三档判定:①档位1(类型标签已说清,如任意 .ts/.kt/.png/.txt)干脆不写字,老版「TypeScript 源代码:程序的一块积木」这类陪跑句全裁;黑话后缀只给 2~4 字范畴词(.md→文档、.css→样式、.yaml→配置、.lock→锁文件、.exe→程序文件),家喻户晓的后缀进「沉默名单」不写字也不许报「没认出」;②档位2(身份唯一无风险,如 src/api/styles)只说「是什么」,≤14 字,直说正脸(src→「项目全部源代码」、api→「供其他程序调用的接口」),人物拟人(体检医生/排版师傅/数据管家)全数退役,黑话给具体例子(services→「业务功能代码,如下单、登录」),唯一留的空间比喻是中间件→「中间站」;③档位3(动错会出事:.env/各种 lock/node_modules/dist/.gitignore)固定公式「是什么+该怎么处理」≤20 字,「不要手动改/不要外传」统一措辞,新增 sticky 标记 —— 这类话重复出现是故意的,要形成「别动它」的条件反射;④状态类像日志播报:空→「空的,还没有内容」、锁定→「系统保护目录,无法查看,不影响使用」、未展开→「还没展开,点击查看」,截断照旧说「至少」;⑤同话降噪:新纯函数 shared/summaryDedup + FileTree 接线 —— 同一目录里一模一样的话从第二条起只亮 emoji(悬停看全文),sticky 豁免;自测按新规整体重写 10 节含降噪豁免断言,npm test 16 套件全绿

- [x] 第九十三锤:界面散装文案按词条规矩过一遍(九十二锤只治了词典,这次治散文)—— 过度解释摘除:文件夹未扫描的提醒从两句并一句(删掉重复的「选中它不会自动扫描…」),「亮还是暗」删掉把三个选项复述一遍的解释,「不劳烦模型」「开门画图」这类内部机制和贫腔退场,路径框/刷新提示/空态提示全部砍到「问题+一步」;术语统一:同一个扫描中状态过去有「正在画地图/扫描中/正在绘制地图」三副面孔,现在按钮统一「扫描中」、整屏等待保留品牌腔「正在绘制地图,稍等」,toast 改「扫描完成」;中文字面收尾:状态栏「N files · N folders」改回中文、「改了啥」全改「改了什么」、删掉「咱们」「才有得」「打个招呼哦」;GitChanges 整摊去拟人(账本/审计官/翻账中→读取记录/正在分析),按钮上的 emoji 图形留给九十五锤统一换;四件套 16 套件全绿
